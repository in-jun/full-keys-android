package dev.injun.fullkeys.theme

import androidx.compose.ui.graphics.Color

// The keyboard's own colours, so the screen that sets it up is the same object as the
// keyboard it shows. #15171C is the ground the keys sit on and #1F2330 the launcher
// background; the blues are the accent a latched Fn key fills with and the lit key on
// the icon.
val Blue10 = Color(0xFF0E1A3A)
val Blue40 = Color(0xFF3D59A1)
val Blue80 = Color(0xFF7AA2F7)
val Blue90 = Color(0xFFDDE3F7)

val Neutral10 = Color(0xFF15171C)
val Neutral20 = Color(0xFF2D3039)
val Neutral40 = Color(0xFF4A5060)
val Neutral60 = Color(0xFF9AA0AD)
val Neutral90 = Color(0xFFE6E8EE)
val Neutral99 = Color(0xFFFBFBFD)
